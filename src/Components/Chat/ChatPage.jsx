import { user } from "./ChatLanding";

const ChatPage = () => {
  return (
    <div>
      <div>
        <h1 className="text-white text-3xl">{ user}</h1>
      </div>
    </div>
  );
};

export default ChatPage;